/**
 * Code block geometry constants.
 *
 * Single source of truth: both layout.ts (geometry arithmetic) and spec.ts
 * (CSS variable emission) read from here, so paint and measurement never drift.
 */
export const CODE_BLOCK_PAD_Y = 10;
export const CODE_BLOCK_PAD_X = 14;
export const CODE_BLOCK_BORDER = 1;
