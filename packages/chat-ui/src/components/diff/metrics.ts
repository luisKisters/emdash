/** Diff row layout constants. */

/** Height of the diff header (matches file-op and execute header heights). */
export const DIFF_HEADER_H = 28;

/** Maximum number of lines shown in the preview (non-scrollable). */
export const DIFF_MAX_LINES = 12;

/** Number of context lines shown before the first change. */
export const DIFF_CONTEXT = 1;

/** Width of the outer border added by the rounded-lg card (top + bottom each 1px). */
export const DIFF_BORDER = 1;

/** Height of the bottom fade overlay shown when the preview is truncated. */
export const DIFF_FADE_H = 24;
