import { DEFAULT_THEME } from '../../../core/theme';
import type { PlanVars } from './plan.def';

export const PLAN_VARS: PlanVars = {
  rowH: DEFAULT_THEME.density.rowH,
  border: 1,
  padX: 8,
  padY: 6,
  iconBox: 14,
  iconGap: 8,
  entryGap: 4,
  windowH: 96,
};
