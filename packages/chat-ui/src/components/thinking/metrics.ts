/** Thinking row layout constants. */
import { BODY } from '../../core/metrics';

/** Height of the collapsed header row: body line-height + 4px top + 4px bottom padding. */
export const THINKING_HEADER_H = BODY.lineHeight + 8;
export const THINKING_WINDOW_H = 72;
export const THINKING_FADE_H = 28;
export const THINKING_PAD_Y = 8;
