/** Execute row layout constants. */

/** Height of the header row. */
export const EXEC_ROW_H = 28;

/** Height of each output line in the expanded body. */
export const EXEC_LINE_H = 18;

/** Top + bottom padding inside the expanded body. */
export const EXEC_PAD_Y = 8;

/** Maximum lines shown before the body scrolls (caps the row height). */
export const EXEC_MAX_LINES = 12;
