/** Tool row layout constants. */
import { BODY } from '../../core/metrics';

/** Estimated height of a collapsed tool row: body line-height + 2px top + 2px bottom padding. */
export const TOOL_ROW_H = BODY.lineHeight + 2;
