import { DEFAULT_THEME } from '../../core/theme';
import type { FileOpVars } from './file-op.def';

export const FILEOP_VARS: FileOpVars = {
  rowH: DEFAULT_THEME.density.rowH,
  padY: 6,
  windowH: 72,
};
