/**
 * Geometry constants for file-op rows.
 *
 * Shared between file-op.def.tsx (measure) and FileOperation.tsx (render slots)
 * so there is a single source of truth for these values.
 */

/** Vertical padding (px) inside the expanded/preview file list. */
export const FILEOP_PAD_Y = 6;
/** Fixed height (px) of the scrollable preview window while running. */
export const FILEOP_WINDOW_H = 72;
