import { Meta, Markdown } from '@storybook/addon-docs/blocks';
import apiDoc from '../../API.md?raw';

{/*
  Renders the canonical public-API doc (packages/chat-ui/API.md) inside
  Storybook so the markdown file stays the single source of truth.
*/}

<Meta title="Docs/Public API" />

<Markdown>{apiDoc}</Markdown>
