---
title: Telemetry
description: What data Emdash collects, what it doesn't, and how to opt out
---

## Summary

- Anonymous usage telemetry, enabled by default
- Explicit allowlisted events only (no autocapture or session recording)
- Sent to [PostHog](https://posthog.com)
- Easy to disable

## Opt Out

### In-App

Settings → General → Privacy & Telemetry → Toggle off

![Disable Telemetry](/media/disabletelemetry.png)

### Environment Variable

```bash
TELEMETRY_ENABLED=false
```

## What We Collect

Anonymous, aggregated usage data only. If you have authenticated with GitHub, your GitHub username may be included with telemetry events to help us understand user engagement patterns.

### App Lifecycle

**`app_started`** / **`app_closed`**

- `app_version`, `electron_version`, `platform`, `arch`, `is_dev`, `install_source`
- `github_username` (if authenticated with GitHub)

**`app_session`** (on quit)

- `session_duration_ms`

**`daily_active_user`** (once per calendar day)

- `date`, `timezone`
- `github_username` (if authenticated with GitHub)

### Feature Usage

Events track which features are used, not what you do with them:

- Navigation and attention (view transitions, focused panel/region, dwell time)
- Core funnel actions (project/task/conversation/PR lifecycle)
- Integrations (connected/disconnected, linked issues)
- Agent runs (started/finished, provider, duration, exit code)
- MCP and skills activity
- Settings and sidebar usage

All events include only: event name, anonymized properties (e.g., `provider: "claude"`). No file paths, code, prompts, or identifying information.

<details>
<summary>**Complete Event List**</summary>

**App Lifecycle:** `app_started`, `app_closed`, `app_window_focused`, `app_session`

**Attention & Views:** `focus_changed`, `home_viewed`, `project_viewed`, `task_viewed`, `settings_viewed`, `skills_viewed`, `mcp_viewed`

**Modals:** `modal_opened`, `modal_closed`

**Projects:** `project_added`, `project_deleted`

**Tasks:** `task_created`, `task_status_changed`, `task_deleted`

**Conversations & Agent Runs:** `conversation_created`, `agent_run_started`, `agent_run_finished`

**PRs:** `pr_created`, `pr_creation_failed`

**Integrations:** `integration_connected`, `integration_disconnected`, `issue_linked_to_task`

**External Tools:** `open_in_external`, `ssh_connection_attempted`

**MCP & Skills:** `mcp_server_added`, `mcp_server_removed`, `skill_installed`, `skill_uninstalled`, `skill_created`

**Settings & Layout:** `setting_changed`, `sidebar_toggled`

**Error/Diagnostics:** `$exception`, `error`, `task_snapshot`, `daily_active_user`, `db_setup`

</details>

### Agent Events

**`agent_run_started`** / **`agent_run_finished`**

- `provider` (e.g., "claude", "codex")
- `duration_ms`
- `exit_code`

## What We Don't Collect

- Code or file contents
- File paths or repository names
- Prompts or messages sent to agents
- Environment variables
- Personally identifiable information (PII) except GitHub username if authenticated
- User text input or command contents
- IP-derived location data
- Session recordings or screen captures

## How It Works

**Anonymous ID:** Random `instanceId` generated on first launch, stored in `${appData}/telemetry.json`. Not linked to any personal information.

**Sanitization:** All data passes through an allowlist. Unexpected or sensitive data is dropped.

**Local development:** `is_dev: true` and `install_source: "dev"` mark development builds.

## For Developers

### Environment Variables

| Variable                  | Default     | Description                     |
| ------------------------- | ----------- | ------------------------------- |
| `TELEMETRY_ENABLED`       | `true`      | Set to `false` to disable       |
| `INSTALL_SOURCE`          | -           | Distribution channel (dmg, dev) |
| `POSTHOG_PROJECT_API_KEY` | Public key  | Override PostHog API key        |
| `POSTHOG_HOST`            | Public host | Override PostHog host           |

PostHog credentials are public and committed in `src/main/appConfig.json`. CI can override via GitHub secrets.

### Adding Events

1. Track only coarse, anonymous metrics
2. Add to allowlist in telemetry service
3. Document here
4. No PII or sensitive data

### Schema Versioning (v0 → v1)

All v1 events include `schema_version: 1`. Historical v0 events have no `schema_version` property.

| v0 event(s)                                                               | v1 event                                   |
| ------------------------------------------------------------------------- | ------------------------------------------ |
| `linear_connected` / `jira_connected`                                     | `integration_connected` (+ `provider`)     |
| `linear_disconnected` / `jira_disconnected`                               | `integration_disconnected` (+ `provider`)  |
| `github_issue_selected` / `jira_issue_selected` / `linear_issue_selected` | `issue_linked_to_task` (+ `provider`)      |
| `toolbar_left_sidebar_clicked` / `toolbar_right_sidebar_clicked`          | `sidebar_toggled` (+ `side`)               |
| `theme_changed` / `telemetry_toggled` / `default_provider_changed`        | `setting_changed` (+ `setting`)            |
| `agent_run_start` / `agent_run_finish`                                    | `agent_run_started` / `agent_run_finished` |

## Questions

- **Code:** `src/main/lib/telemetry.ts`, `src/main/core/telemetry/controller.ts`
- **Issues:** [GitHub Issues](https://github.com/generalaction/emdash/issues)
- **Discuss:** [GitHub Discussions](https://github.com/generalaction/emdash/discussions)
